package com.glority.quality.connectors.supportdb;

import java.util.List;

import com.glority.quality.model.SupportTicket;

/**
 * . This is the general interface for a operations support DB A support DB is
 * mainly used for operations team to handle the tickets in daily operations and
 * supports
 * 
 * So we can abstract a support ticket with following types
 * 
 * @author Xu Qingsong
 * 
 */
public interface SupportDBClient {
    /**
     * Get the list of support ticket;
     * 
     * @return List<SupportTicket>
     */
    List<SupportTicket> get();

}

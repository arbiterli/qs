/*
 * update build_metric test_success_rate value. you must be careful that only the old data should be updated.
 * */
UPDATE `qualitysystem`.build_metric b SET b.newvalue=b.newvalue*100 WHERE b.name LIKE '%_TEST_SUCCESS_RATE'
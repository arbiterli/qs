package com.glority.qualityserver.manager;

import com.glority.qualityserver.model.TestGroup;

/**
 * TestGroup manager Interface.
 * 
 * @author liheping
 * 
 */
public interface TestGroupManager extends GenericManager<TestGroup, Long> {
    /**
     * get the latest test group for build.
     * 
     * @param buildId
     * @return
     */
    TestGroup getLatestTestGroup(long buildId);
}

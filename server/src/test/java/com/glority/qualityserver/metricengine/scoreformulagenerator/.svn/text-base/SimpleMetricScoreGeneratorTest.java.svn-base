package com.glority.qualityserver.metricengine.scoreformulagenerator;


/**
 * Used for testing the Metric Score build generator for
 * SimpleMetricScoreFormula.
 * 
 * <p>
 * Simple metric score means the score has no sub score item, such as JAVA MI,
 * checkstyle.
 * </p>
 * 
 * @author Chenhong
 * 
 */
public class SimpleMetricScoreGeneratorTest {
  //since the formula is not used now
    
//    private static final int SIZE_3 = 3;
//
//    /**
//     * Test with JAVAMI.
//     */
//    @Test
//    public void testJAVAMI() {
//        String metricName = "JAVA MI";
//        MetricScoreFormula formula = MetricEngine.getScoreItemFormula(metricName);
//
//        assertTrue(formula instanceof SimpleMetricScoreFormula);
//        SimpleMetricScoreFormula simpleFormula = (SimpleMetricScoreFormula) formula;
//
//        assertTrue(simpleFormula.getPower() == 0);
//
//        assertEquals(SIZE_3, simpleFormula.getScoreFormulas().size());
//
//        assertFalse(simpleFormula.isRated());
//
//        ScoreFormula scoreFormula = simpleFormula.getScoreFormulas().get(0);
//
//        assertEquals("0", scoreFormula.getValue());
//    }
//
//    /**
//     * Test for defective metrics.
//     */
//    @Test
//    public void testJAVADEFECTIVE() {
//        String metricName = "JAVA DEFFECTS";
//
//        MetricScoreFormula formula = MetricEngine.getScoreItemFormula(metricName);
//
//        assertTrue(formula instanceof SimpleMetricScoreFormula);
//        SimpleMetricScoreFormula simpleFormula = (SimpleMetricScoreFormula) formula;
//
//        assertTrue(simpleFormula.getPower() == 0);
//
//        assertEquals(SIZE_3, simpleFormula.getScoreFormulas().size());
//
//        assertTrue(simpleFormula.isRated());
//
//        ScoreFormula scoreFormula = simpleFormula.getScoreFormulas().get(0);
//
//        assertEquals("100", scoreFormula.getValue());
//    }
}
